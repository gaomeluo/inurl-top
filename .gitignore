/public/
/resources/
.hugo_build.lock
.DS_Store
# 服务器导出备份（含 40MB+ 上传文件），仅在本地留作备份，不进 git
_server_export/
# Hugo 本地二进制（如本地校验用）
/.hugo/

# 工作区记忆（迁移笔记/凭据提示），不进公开仓库
.workbuddy/

# 主题源包（仅用于本地复刻参考，不进版本库）
_theme_src/

# 本地 Hugo 二进制 / 下载包 / 构建缓存（仅本地校验用，不进公开仓库）
hugo.exe
_hugo*.zip
/.hugo/
